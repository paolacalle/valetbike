<div class="access new">
  <h2>Welcome</h2>
  <p>Sign Up</p>

  <%= form_tag(users_path) do %>
    <table summary="login">
      <tr>
        <th><%= label_tag(:first_name) %></th>
        <td><%= text_field_tag(:first_name, params[:first_name]) %></td>
      </tr>
      <tr>
        <th><%= label_tag(:last_name) %></th>
        <td><%= text_field_tag(:last_name, params[:last_name]) %></td>
      </tr>
      <tr>
        <th><%= label_tag(:gmail_address) %></th>
        <td><%= text_field_tag(:gmail_address, params[:gmail_address]) %></td>
      </tr>
      <tr>
        <th><%= label_tag(:password) %></th>
        <td><%= password_field_tag(:password) %></td>
      </tr>
    </table>

    <div class="form-buttons">
      <%= submit_tag("Sign Up") %>
    </div>

  <% end %>
</div>
