# when ryan was messing around with the flashes, they deleted this file. they restored it after playing around, but may conflict with things
<% if object && object.errors.size > 0 %>
  logger.log("OKAY so obj exists and there are errors")
  <div id="error-explanation">
		<h2><%= pluralize(object.errors.size, 'error') %> prohibited this record from being saved</h2>
		<p>There were problems with the following fields:</p>
		<ul>
		<% object.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
	</div>
<% end %>
