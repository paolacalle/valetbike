
<div class="record-row <%= cycle 'odd', 'even', name: 'row-cycler' %> flexbox vertical stretch">
  <div>
    <a><%= station.identifier %>: <%= station.name %> (<%= station.address %>)</a>
    <a><%= station.docked_bikes.count %> bikes</a>
    <%= link_to "Select Station", bikes_path(selected_station_id: station.id), class: "fill-block"%>
  </div>
</div>

